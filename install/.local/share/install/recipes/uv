#!/usr/bin/env bash
set -euo pipefail

UV="$HOME/.local/bin/uv"

if [ -x "$UV" ]; then
  echo "Updating uv..."
  "$UV" self update
else
  echo "Installing latest uv..."
  UV_NO_MODIFY_PATH=1 curl -fsSL https://astral.sh/uv/install.sh | sh
fi

echo "uv $("$UV" --version) installed"
