#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

run() {
  echo ""
  echo "==> $1"
  "$SCRIPT_DIR/$1"
}

# base tools
run starship
run go
run rust
run uv
run ruff
run bun
run gh
run aws
run ssm
run ripgrep
run shellcheck
run shfmt
run fzf
run bat
run just
run docker
run 1password
run op
run gopass
run proton_pass

run fnm

run claude
run codex

run opencode
run neovim
run ghostty
run tmux
run brave
run lmstudio

echo ""
echo "All tools installed successfully."
