#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

LATEST=$(github_latest sharkdp/bat)
LATEST="${LATEST#v}"

INSTALLED=$(batcat --version 2>/dev/null | awk '{print $2}') || INSTALLED="not installed"

check_version bat "$INSTALLED" "$LATEST"
