#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

LATEST=$(github_latest casey/just)

INSTALLED=$(just --version 2>/dev/null | awk '{print $2}') || INSTALLED="not installed"

check_version just "$INSTALLED" "$LATEST"
