#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

LATEST=$(github_latest junegunn/fzf)
LATEST="${LATEST#v}"

INSTALLED=$(fzf --version 2>/dev/null | awk '{print $1}') || INSTALLED="not installed"

check_version fzf "$INSTALLED" "$LATEST"
