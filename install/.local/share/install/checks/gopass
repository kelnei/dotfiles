#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

LATEST=$(github_latest gopasspw/gopass)
LATEST="${LATEST#v}"

INSTALLED=$(gopass version 2>/dev/null | head -1 | awk '{print $2}') || INSTALLED="not installed"

check_version gopass "$INSTALLED" "$LATEST"
